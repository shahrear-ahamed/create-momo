---
"create-momo": minor
---

feat: transform into a context-aware monorepo manager with `dev`, `build`, `lint`, and `start` commands. Added safety checks to prevent nested monorepos and streamlined the creation flow.
