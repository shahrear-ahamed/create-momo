---
"create-momo": minor
---

Refine add command logic with standardized action flags (-a, -p, -d) and renamed targeting flags (--to-app, --to-pkg). Fix Turbo integration by detecting specific package manager versions and improving the runTurbo wrapper.
