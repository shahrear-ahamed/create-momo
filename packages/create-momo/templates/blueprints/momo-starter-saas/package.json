{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "check": "oxlint . && oxfmt --check .",
    "format": "oxfmt .",
    "clean": "turbo clean",
    "test": "turbo test"
  },
  "devDependencies": {
    "create-momo": "^0.6.1",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "turbo": "^2.8.13",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "{{packageManager}}@{{pmVersion}}"
}
