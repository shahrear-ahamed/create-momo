{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "check": "oxlint . && oxfmt --check .",
    "format": "oxfmt .",
    "clean": "turbo clean",
    "test": "turbo test"
  },
  "devDependencies": {
    "create-momo": "latest",
    "oxfmt": "latest",
    "oxlint": "latest",
    "turbo": "^2.8.12",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "{{packageManager}}@{{pmVersion}}"
}
